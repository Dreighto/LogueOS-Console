node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Per-worktree MCP config (written by dispatch_listener at spawn time, never tracked).
# Without this, verifyWorktreeParked rejects the worktree as dirty (untracked
# .mcp.json) and dispatch fails with pre_spawn_dirty_refusal. Mirror project-miru.
.mcp.json
mcp.json
